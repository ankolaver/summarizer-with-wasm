/* 
pub use rust_bert::pipelines::summarization::SummarizationModel;

pub fn summarize(input_text: &str) -> Result<Vec<String>, anyhow::Error> {
    let model = SummarizationModel::new(Default::default())?;
    let output = model.summarize([&input_text[..]]);
    Ok(output)
}
*/

